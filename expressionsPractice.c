#include <stdio.h>

int main(void){
    int x = 7, y = 24, z = 8, a = 4, s = 6, d = 18, c = 3, f = 2, v = 5, g = 
    printf("%d", (int)x - (int)y / (int)z * (int)a + (int)s);
    return 0; 
}
// turn every variable into an equation.